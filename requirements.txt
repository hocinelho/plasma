@"
# --- Web backend ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
python-multipart>=0.0.20
python-dotenv>=1.0.1

# --- HTTP client for cloud LLMs ---
httpx>=0.28.0

# --- Data & validation ---
pydantic>=2.10.0

# --- Memory (SQLite FTS5 comes built-in with Python) ---
# sqlite-vec will be added at Step 3

# --- Local LLM (added at Step 4) ---
# ollama>=0.4.0

# --- ASR (added at Step 5) ---
# faster-whisper>=1.1.0

# --- TTS (added at Step 6) ---
# piper-tts>=1.2.0

# --- Dev ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
"@ | Out-File -FilePath requirements.txt -Encoding utf8